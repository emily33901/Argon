using System;

// Autogenerated @ 11/08/18
namespace InterfaceArgonCore
{
    /// <summary>
    /// Implements the map for interface MappedTest001
    /// </summary>
    [ArgonCore.Interface.Map(Name = "MappedTest001")]
    public class MappedTest001_Map : ArgonCore.Interface.IBaseInterfaceMap
    {
        public int PointerTest(IntPtr _, ref int a, ref int b, ref int c)
        {

            var result = Client.ClientPipe.CallSerializedFunction(PipeId, new ArgonCore.IPC.SerializedFunction()
            {
                ClientId = ClientId,
                InterfaceId = InterfaceId,
                Name = "PointerTest",
                Args = new object[] {a, b, c},

            });

            a = (int)result.Args[0];
            b = (int)result.Args[1];
            c = (int)result.Args[2];


            return (int)result.Result;
        }
        public int BufferTest(IntPtr _, IntPtr b_pointer, int b_length)
        {
            var b = new ArgonCore.Util.Buffer();
            b.ReadFromPointer(b_pointer, b_length);


            var result = Client.ClientPipe.CallSerializedFunction(PipeId, new ArgonCore.IPC.SerializedFunction()
            {
                ClientId = ClientId,
                InterfaceId = InterfaceId,
                Name = "BufferTest",
                Args = new object[] {b},

            });

            b = (ArgonCore.Util.Buffer)result.Args[0];

            b.WriteToPointer(b_pointer, b_length);

            return (int)result.Result;
        }
        public void StructTest(IntPtr _, IntPtr b_pointer, int b_length)
        {
            var b = new ArgonCore.Util.Buffer();
            b.ReadFromPointer(b_pointer, b_length);


            var result = Client.ClientPipe.CallSerializedFunction(PipeId, new ArgonCore.IPC.SerializedFunction()
            {
                ClientId = ClientId,
                InterfaceId = InterfaceId,
                Name = "StructTest",
                Args = new object[] {b},

            });

            b = (ArgonCore.Util.Buffer)result.Args[0];

            b.WriteToPointer(b_pointer, b_length);

        }
    }
}
